<UserControl x:Class="PizzaShed.Views.Controls.DriverCardUserControl"
                                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:PizzaShed.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:OrderViewModel}"
             mc:Ignorable="d"  
            d:DesignHeight="400" d:DesignWidth="300">

    <Border BorderBrush="{DynamicResource MaterialDesignDarkForeground}"
            BorderThickness="2" 
            CornerRadius="8" 
            Background="{DynamicResource MaterialDesignDarkSeparatorBackground}"
            Margin="10" 
            Padding="10"
            Width="300" 
            Height="400">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,10">

                <TextBlock Text="{Binding Path=ID, StringFormat='Order # {0000}'}" 
                           FontSize="36" FontWeight="Bold" 
                           HorizontalAlignment="Center" Foreground="{DynamicResource MaterialDesignDarkSeparatorBackground}"/>

            </StackPanel>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">

                <ItemsControl ItemsSource="{Binding Path=OrderProducts}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,5,0,5" >

                                <TextBlock Text="{Binding Path=TicketName}" 
                                           FontSize="18"                                            
                                           TextWrapping="Wrap" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </ScrollViewer>

            <Button Grid.Row="2"
                    Style="{StaticResource MaterialDesignRaisedAccentButton}"
                    HorizontalAlignment="Stretch"
                    Margin="0,10,10,10"
                    
                    Command="{Binding DataContext.CompletePrepCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ItemsControl}}}"
                    CommandParameter="{Binding .}">

                <TextBlock Text="Start Order" FontSize="20"/>
            </Button>

        </Grid>
    </Border>
</UserControl>